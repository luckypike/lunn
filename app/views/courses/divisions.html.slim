- set_meta_tags \
  title: @node.title

= react_component 'Courses/Divisions', { \
  node: @node.as_json(only: :title),
  loaf: @node.loaf.as_json(only: %i[mlid depth], methods: %i[title path]),
  level: params[:level],
  divisions: DivisionSerializer.new(@divisions, { include: %i[courses] }).serializable_hash,
  # divisionss: @divisions.as_json( \
  #   include: { \
  #     image: { \
  #       only: [],
  #       methods: :path \
  #     }, \
  #     division_courses: { \
  #       only: %i[title nid],
  #       methods: %i[desc path spec level youtube ege price_1 price_2 price_3 time_1 time_2 time_3 places_1 places_2 places_3]} \
  #     }, \
  #   methods: %i[desc levels],
  #   only: %i[title nid] \
  # ),
}, { prerender: Rails.env.production? }
